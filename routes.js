const authRouter = require("./routes/authRouter")
const jobsRouter = require("./routes/jobsRouter")
const userRouter = require("./routes/userRouter")

const routes = { authRouter, userRouter, jobsRouter }

module.exports = routes
